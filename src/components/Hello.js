import React from 'react'

const Hello = (props) => {
    return(
        <div>
            <h1>
                So I see we meet again {props.name}
            </h1>
        </div>
    )

}

export default Hello;